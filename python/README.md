# asb-security-schema (Python SDK)

Helpers for loading and validating ASB Security Schema events.

Most users should install the package directly from this repository:

```sh
pip install git+https://github.com/asb-security/asb-security-schema.git#subdirectory=python
```

See the root README for full schema docs.

